# Backlog

当前没有已接受的非阻塞审查项。

## 设计阶段的外部不确定性

- 上游新房/二手房筛选请求的真实参数、分页和 JSON schema 尚未在当前设计阶段确认；由 `search-005` 在不绕过 WAF/验证码的前提下验证。
- 租赁房源列表接口没有公开证据；第一版只交付 FAQ/资料/办理入口。
- PinMe CLI 当前未安装，远端项目名和登录状态未知；由 `bootstrap-001` 与 `integration-008` 处理。
